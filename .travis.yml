language: php

sudo: false

cache:
  directories:
    - $HOME/.composer/cache

php:
  - 7.1
  - 7.2
  - 7.3

before_install:
  - composer self-update

install:
  - composer install

before_script:

script:
  - vendor/bin/phpunit
  - vendor/bin/phpcs src --standard=PSR2
  - vendor/bin/phpcs Tests --standard=PSR2


